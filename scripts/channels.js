// channels.js
// Funciones para manejar la lista de canales

function loadChannels(serverId) {
    // Cargar la lista de canales del servidor seleccionado desde el backend
}

// Define una variable para almacenar el ID del canal actual
let currentChannelId = null;

function selectChannel(channelId) {
    // Cargar los mensajes del canal seleccionado
    currentChannelId = channelId;
}

function createChannel() {
    // Crear un nuevo canal en el servidor seleccionado y actualizar la lista
}
